spring:
  application:
    name: market
  datasource:
    url: jdbc:h2:mem:market;DB_CLOSE_ON_EXIT=TRUE
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  h2:
    console:
        enabled: true
